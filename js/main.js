'use strict';

// 定数を宣言し、要素を取得する
const ul = document.getElementById("unorderd-list");
const li = document.getElementById("unorderd-list__list");
const submitButtonAdd = document.getElementById("submit-button-add");
const submitButtonDelete = document.getElementById("submit-button-delete");

// Addボタンが押された時
submitButtonAdd.addEventListener("submit", (event) => {

  // リストに追加する

});



// Deleteボタンが押された時
submitButtonDelete.addEventListener("submit", (event) => {

  // リストから削除する

});

